NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Fallback temporário para projetos que ainda usam a chave anon antiga.
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Opcional enquanto módulos legados do catálogo são migrados para a Data API.
# Autenticação e autorização nunca dependem desta conexão direta.
DATABASE_URL=
DATABASE_POOL_SIZE=

# Armazenamento local é apenas fallback de desenvolvimento.
LOCAL_STORAGE_DIR=
STORAGE_PUBLIC_BASE_URL=
STORAGE_SIGNING_SECRET=
